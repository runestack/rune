name: Rune CI

on:
  # push on default branches only; PR updates use pull_request (avoids double runs)
  push:
    branches: [ main, master, dev ]
  pull_request:
    branches: [ main, master, dev, 'feat/**' ]
  workflow_dispatch:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # The complexity gate needs a merge base. Without one, revgrep cannot
          # filter and every pre-existing offender in the tree is reported, so
          # a shallow clone fails the job with ~200 unrelated findings.
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
          check-latest: true
          cache: true
      - name: Install tools
        run: make setup
      - name: Lint
        # Judge a stacked branch against its own parent, not against dev. The
        # ref goes through the environment: refnames may contain shell
        # metacharacters, and interpolating one into `run:` would execute them.
        env:
          BASE_REF: ${{ github.base_ref || 'dev' }}
        run: make lint COMPLEXITY_BASE="origin/$BASE_REF"

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
          check-latest: true
          cache: true
      # Seed the image the docker runner tests use. Shared runners hit Docker
      # Hub's anonymous pull rate limit ("toomanyrequests"), so pull from the
      # ECR Public mirror of the Docker official images (no anonymous limit)
      # and retag; fall back to Docker Hub if the mirror is unavailable. The
      # tests use imagePull=missing, so a seeded image means zero Hub pulls.
      - name: Seed test images (avoid Docker Hub rate limits)
        run: |
          if docker pull public.ecr.aws/docker/library/alpine:latest; then
            docker tag public.ecr.aws/docker/library/alpine:latest alpine:latest
          else
            for i in 1 2 3; do docker pull alpine:latest && break || sleep $((i * 15)); done
          fi
      - name: Run unit tests with race detection
        run: make test-unit-race
      - name: Check Coverage
        env:
          COVERAGE_THRESHOLD: ${{ secrets.COVERAGE_THRESHOLD }}
        run: make check-coverage

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
          check-latest: true
          cache: true
      - name: Build
        run: make build

  docker-build:
    name: Docker Build (sanity)
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build image
        run: docker build --no-cache -t local/rune:ci .

